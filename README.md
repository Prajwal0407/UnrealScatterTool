# UnrealScatterTool

**UnrealScatterTool** is a lightweight and efficient **static mesh scattering/array placement tool** for Unreal Engine. It lets you quickly place multiple static mesh instances in your scene in custom patterns — speeding up environment layout and level design workflows.

🚀 Designed for fast iterative placement of mesh arrays without the overhead of foliage tools or procedural systems.

## 📌 Features

- 🎯 **Array-Based Static Mesh Placement** – Scatter mesh instances in grid or custom arrangements.
- ⚡ **Fast and Lightweight** – Minimal overhead, easy to integrate into existing projects.
- 🧰 **Blueprint-Ready** – Built with UE-native Blueprint support (if applicable — adjust as needed).
- 🛠️ Designed to quickly populate scenes with props and assets for level art or prototyping.


## 📥 Installation

⚠️ This assumes you’re using Unreal Engine 5.x (adjust version if different).

1. Clone this repository into your Unreal project’s `Plugins/` directory:
   ```bash
   git clone https://github.com/Prajwal0407/UnrealScatterTool.git Plugins/UnrealScatterTool
